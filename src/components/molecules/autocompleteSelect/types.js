/**
 * @typedef {object} Option
 * @property {string | number | null} value - The value of the option.
 * @property {string} label - The label of the option.
 * @property {string} [city] - Optional city metadata.
 * @property {string} [context] - Optional context metadata.
 * @property {string} [postcode] - Optional postcode metadata.
 * @property {string} [provider] - Optional provider key.
 * @property {string | null} [providerId] - Optional provider identifier.
 * @property {boolean} [isHeader] - Optional group header marker.
 */
