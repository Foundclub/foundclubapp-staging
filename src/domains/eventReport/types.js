/**
 * Data required to create an Event Report.
 * @typedef {object} CreateEventReportData
 * @property {string} event - The ID of the event being reported.
 * @property {string} reason - The reason for the report.
 */
